# kube-exec-rs

# help

```text
$ ./target/debug/kube-exec-rs.exe --help
Usage: kube-exec-rs.exe [OPTIONS]

Options:
  -s <text>      script text to run, either text or file be set
  -f <file>      script file to run, either text or file be set
  -h, --help     Print help
  -V, --version  Print version
```
